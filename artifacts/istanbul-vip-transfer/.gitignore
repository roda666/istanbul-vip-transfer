# Next.js build output — kept separate per-mode to prevent chunk collisions
# next dev  → .next-dev   (NODE_ENV=development)
# next build → .next      (NODE_ENV=production)
.next/
.next-dev/

# Playwright test artifacts
test-results/
playwright-report/
