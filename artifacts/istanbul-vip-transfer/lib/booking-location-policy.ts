/**
 * Locations that must not be offered by the public reservation form.
 *
 * Keep this list limited to places the business has explicitly confirmed it
 * cannot serve. It affects public pickup/dropoff choices only.
 */
export const PUBLICLY_UNAVAILABLE_BOOKING_LOCATION_SLUGS = ['adalar'] as const;